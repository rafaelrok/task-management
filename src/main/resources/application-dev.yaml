# Application Configuration
spring:
  application:
    name: task-management

  # Multipart Configuration
  servlet:
    multipart:
      enabled: true
      max-file-size: 5MB
      max-request-size: 10MB
      file-size-threshold: 1MB
    encoding:
      charset: UTF-8
      enabled: true
      force: true

  # Database Configuration
  datasource:
    url: jdbc:postgresql://localhost:5433/tasks?characterEncoding=UTF-8&useUnicode=true
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
    hikari:
      pool-name: TaskManagementHikariPool
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 300000
      max-lifetime: 1800000
      leak-detection-threshold: 60000
      auto-commit: true
      initialization-fail-timeout: 60000

  # JPA Configuration
  jpa:
    hibernate:
      ddl-auto: validate
    defer-datasource-initialization: false
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          batch_size: 20
        order_inserts: true
        order_updates: true
        generate_statistics: false
        connection:
          provider_disables_autocommit: true

  # Flyway Configuration
  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: 0
    locations: classpath:db/migration
    validate-on-migrate: true
    clean-disabled: false
    out-of-order: false

  # Jackson Configuration
  jackson:
    serialization:
      indent-output: true
    deserialization:
      fail-on-unknown-properties: false

  # Cache Configuration
  cache:
    type: simple

  # Security (básica)
  security:
    user:
      name: admin
      password: admin

  # Thymeleaf Configuration
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML

# Server Configuration
server:
  port: 8080
  compression:
    enabled: true
  http2:
    enabled: true
  tomcat:
    max-swallow-size: 10MB
    max-http-form-post-size: 10MB
    max-parameter-count: 1000
    uri-encoding: UTF-8
    threads:
      max: 200
      min-spare: 10
    connection-timeout: 30s
    accept-count: 100
    # Fix FileCountLimitExceededException - permite múltiplos campos de formulário
    max-file-count: 100


# Management/Actuator Configuration
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: when-authorized
  prometheus:
    metrics:
      export:
        enabled: true

# Logging Configuration
logging:
  level:
    root: INFO
    com.example.taskmanagement: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

# SpringDoc OpenAPI Configuration
springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html

# Custom Application Properties
app:
  database:
    max-pool-size: 20
    min-idle: 5
    connection-timeout: 30000
    idle-timeout: 300000
    max-lifetime: 1800000
  cache:
    enabled: true
    ttl: 3600
    max-size: 1000
  api:
    version: 1.0.0
    rate-limit: 100
    enable-swagger: true
  timer:
    fixedDelayMs: 10000
